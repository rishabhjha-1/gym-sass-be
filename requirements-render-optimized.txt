# Render-optimized requirements for face recognition
# Uses pre-compiled wheels to minimize build memory usage

# Core dependencies (pre-compiled)
numpy==1.24.3
Pillow==9.5.0
requests==2.31.0

# FastAPI (minimal)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Face recognition with specific dlib version
# This version has better pre-compiled wheel support
dlib==19.24.2
face-recognition==1.3.0

# OpenCV headless (pre-compiled)
opencv-python-headless==4.8.1.78 