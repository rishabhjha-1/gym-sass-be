# Requirements using pre-compiled dlib wheels
# This avoids compilation issues on Render

# Core dependencies
numpy==1.24.3
Pillow==9.5.0
requests==2.31.0

# FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Pre-compiled dlib wheel (no compilation needed)
dlib @ https://github.com/sachadee/Dlib/releases/download/v19.22/dlib-19.22.99-cp311-cp311-linux_x86_64.whl

# Face recognition (will use the pre-compiled dlib)
face-recognition==1.3.0

# OpenCV headless
opencv-python-headless==4.8.1.78 