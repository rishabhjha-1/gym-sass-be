# Lightweight requirements for face recognition service
# Optimized for smaller build size on Render

# Core face recognition - use specific version to avoid large dependencies
face-recognition==1.3.0

# Use opencv-python-headless instead of full opencv-python (saves ~200MB)
opencv-python-headless==4.8.1.78

# Optimized numpy version
numpy==1.24.3

# Lightweight image processing
Pillow==9.5.0

# HTTP requests
requests==2.31.0

# FastAPI with minimal dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Optional: Use aiofiles for async file operations if needed
# aiofiles==23.2.1 